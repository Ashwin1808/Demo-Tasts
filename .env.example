# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=taskdb
DB_USERNAME=postgres
DB_PASSWORD=postgres

# For local dev without Docker (backend connects directly to localhost)
# DB_HOST=localhost

# Backend
SERVER_PORT=8080

# Frontend (Vite)
VITE_API_URL=http://localhost:8080
